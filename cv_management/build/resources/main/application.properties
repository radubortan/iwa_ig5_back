# Minio Host
spring.minio.url="http://127.0.0.1:9000"
# Minio Bucket name for your application
spring.minio.bucket=test-vincent
# Minio access key (login)
spring.minio.access-key=eYRsoaxwyKhPI6Ju
# Minio secret key (password)
spring.minio.secret-key=bbz2QBog0uqo45O31taWhfnmDY3EzkSH

## PostgreSQL
spring.datasource.url= jdbc:postgresql://localhost:5432/fsj
spring.datasource.username= fsj
spring.datasource.password= fsjAdmin2022

## JPA
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation= true
spring.jpa.properties.hibernate.dialect= org.hibernate.dialect.PostgreSQLDialect

# Hibernate ddl auto (create, create-drop, validate, update)
spring.jpa.hibernate.ddl-auto= update